# Решение: стабильность #176 vs #177 (2026-09-10)

## Вопрос
Что стабильнее — #176 или #177 — и применить более стабильную версию.

## Проверенные факты
| PR | Тест-джобы CI | Что делает | Итог |
|----|---------------|------------|------|
| #176 `d5c4905` | **pass** (ubuntu + macos) | pre-spawn подмена заведомо-битых Claude-моделей | СТАБИЛЬНА |
| #177 `05b1976` | **fail** (ubuntu + macos) | replay последнего необработанного user_message на респавне адаптера | СЛОМАНА |

## Почему #177 сломана
1. Падает `web/server/ws-bridge.test.ts › handleCLIMessage: system.init does not re-flush already-sent messages` — на обеих ОС (не флейк). Регрессия его же фичи: повторная отправка нарушает инвариант «не пере-флашить уже отправленное».
2. Противоречит принятому в плане решению: зависшее in-flight `user_message` НЕ переотправлять автоматически (авто-повтор side-effecting операции опаснее потери).

## Состояние на момент решения
- Прод-процесс стартовал 07:14:11 UTC, #177 влит 07:18:13 UTC → **живой прод уже крутит стабильный код уровня #176**; сломанный #177 в исполнение НЕ попал.
- Риск: следующий автодеплой/рестарт поднимет #177 с регрессией.

## Применённое действие
Revert #177 (`05b1976`) из `main` через revert-PR (чисто: верхушка main, +228 строк только в `claude-adapter.ts(+.test)`, конфликтов нет). После этого `main` == поведение #176. Параллельная ветка `feat/replay-last-user-message-on-respawn` не тронута — исправленный #177 может быть перевнесён позже поверх реверта.

## Критерий готовности
- [ ] `main` не содержит #177 (revert смержен, CI зелёный).
- [ ] Прод при следующем рестарте поднимает стабильный код (#176-уровень), тест `system.init does not re-flush` — зелёный.

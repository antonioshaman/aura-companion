node_modules/
dist/
*.tsbuildinfo
.DS_Store
bun.lockb
bun.lock
# Task 15b: track the workspace lockfile so CI's --frozen-lockfile
# actually gates dependency drift. The root-level bun.lock is stale
# from the pre-fork repo and stays ignored.
!web/bun.lock
package-lock.json
test-live.ts
web/dist/
.env
web/.env.local
.dev-*.pid
.dev-*.log
docs/mocks/
docs/repo-only/
scripts/test-codex-*.ts
web/dev-dist/
bun.log
web/bun.log
.aura-server.pid
